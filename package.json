{
  "name": "restify-sequelize-query",
  "version": "1.0.5",
  "description": "Create Restify middleware that queries Sequelize models.",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "mocha"
  },
  "author": "Russ Panula <russ@dfa.net>",
  "license": "MIT",
  "dependencies": {
    "circular-json": "^0.3.3",
    "redis": "^2.8.0",
    "restify": "^5.2.0",
    "restify-errors": "^5.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "sequelize-mock": "^0.9.1"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "current"
          }
        }
      ]
    ]
  },
  "repository": "dfadev/restify-sequelize-query"
}
